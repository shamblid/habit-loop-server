{
    "dynamodb": {
        "habitTable": "habit-records",
        "userTable": "user-records",
        "region": "localhost",
        "endpoint": "http://localhost:8000",
        "accessKeyId": "DEFAULT_ACCESS_KEY",  // needed if you dont have aws credentials at all in env
        "secretAccessKey": "DEFAULT_SECRET"
    }
}